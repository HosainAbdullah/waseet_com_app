import 'package:flutter/material.dart';

class ModelHome {
  final IconData icon;
  final String title;
  final Color colors;
  final dynamic rout;

  ModelHome(this.icon, this.title, this.colors, this.rout);
}
